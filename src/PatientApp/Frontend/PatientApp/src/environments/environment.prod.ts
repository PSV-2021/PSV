export const environment = {
  production: true,
  apiUrl: location.hostname,
  port: "80"
};
/*export const environment = {
  production: true,
  apiUrl: "${HOSPITAL_API}",
  port: "${HOSPITAL_API_PORT}"
};*/
