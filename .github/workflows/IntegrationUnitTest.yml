name: IntegrationUnitTests

on:
  push:
    paths: 
      - "src/Integration/**"
      - "src/\"Integration API\"/**"
      - "src/IntegrationApiTests/**"
jobs:
  build:
    runs-on: windows-latest
    strategy:
      matrix:
        dotnet-version: [ '3.1.x' ]

    steps:
      - uses: actions/checkout@v2
      - name: Setup .NET Core SDK ${{ matrix.dotnet-version }}
        uses: actions/setup-dotnet@v1.7.2
        with:
          dotnet-version: ${{ matrix.dotnet-version }}
      - name: Install dependencies
        run: dotnet restore src/IntegrationAPI
      - name: Build
        run: dotnet build src/IntegrationAPI --configuration Release --no-restore
      - name: Test
        run: dotnet test src/IntegrationAPI --filter Type=UnitTest --no-restore --verbosity normal